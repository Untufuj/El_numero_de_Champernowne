/**
 * This is the main file for your project.
 *
 * Create images, tilemaps, animations, and songs using the
 * asset explorer in VS Code. You can reference those assets
 * using the tagged templates on the assets namespace:
 *
 *     assets.image`myImageName`
 *     assets.tilemap`myTilemapName`
 *     assets.tile`myTileName`
 *     assets.animation`myAnimationName`
 *     assets.song`mySongName`
 *
 * New to MakeCode Arcade? Try creating a new project using one
 * of the templates to learn about Sprites, Tilemaps, Animations,
 * and more! Or check out the reference docs here:
 *
 * https://arcade.makecode.com/reference
 */

game.onUpdate(() => {
    // Code in this function will run once per frame. MakeCode
    // Arcade games run at 30 FPS
    // so this function will run 30 times per second.
    // You can use this function to update the game state,
    // check for collisions, and more.
    // For example, you can use the following code to move a sprite
    // to the right when the right arrow key is pressed:
    // if (controller.right.isPressed()) {
    //     mySprite.x += 1
    // }
    // You can also use this function to check for collisions
    // between sprites. For example, you can use the following code
    // to check if two sprites are overlapping:
    // if (mySprite.overlapsWith(otherSprite)) {
    //     // Do something when the sprites overlap
    // }
    // You can also use this function to check for collisions
    // between sprites and the edges of the screen. For example,
    // you can use the following code to check if a sprite is
    // touching the edge of the screen:
    // if (mySprite.isHittingTile(CollisionDirection.Bottom)) {
    //     // Do something when the sprite is touching the bottom edge
    // }
    // You can also use this function to check for collisions
    // between sprites and the edges of the screen. For example,
    // if (mySprite.isHittingTile(CollisionDirection.Left)) {
    //     // Do something when the sprite is touching the left edge
    // }
    // You can also use this function to check for collisions
    // between sprites and the edges of the screen. For example,
    // if (mySprite.isHittingTile(CollisionDirection.Right)) {
    //     // Do something when the sprite is touching the right edge
    // }
    // You can also use this function to check for collisions
    // between sprites and the edges of the screen. For example,
    // if (mySprite.isHittingTile(CollisionDirection.Top)) {
    //     // Do something when the sprite is touching the top edge
    // }
    // You can also use this function to check for collisions
    // between sprites and the edges of the screen. For example,
    // if (mySprite.isHittingTile(CollisionDirection.BottomLeft)) {
    //     // Do something when the sprite is touching the bottom left edge
    // }
    // You can also use this function to check for collisions
    // between sprites and the edges of the screen. For example,
    // if (mySprite.isHittingTile(CollisionDirection.BottomRight)) {
    //     // Do something when the sprite is touching the bottom right edge
    // }
    // You can also use this function to check for collisions


    

});
